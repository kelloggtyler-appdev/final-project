<h1>
  Shift Details
</h1>

<% if current_user.seniority == "Chief" %>
<a href="/shifts/<%= @shift.id %>/edit">
  Edit shift
</a>

<hr>

<% end %>


<dl>
  <dt>
    Resident
  </dt>
  <dd>
    <%= User.find(@shift.user_id).first_name %> <%= User.find(@shift.user_id).last_name %>
  </dd>

  <dt>
    Service
  </dt>
  <dd>
    <%= Service.find(@shift.service_id).name %>
  </dd>

  <dt>
    Date
  </dt>
  <dd>
    <%= @shift.date.strftime("%m/%d/%y") %>
  </dd>

  <dt>
    Night
  </dt>
  <dd>
    <%= @shift.night %>
  </dd>

  <dt>
    Created at
  </dt>
  <dd>
    <%= time_ago_in_words(@shift.created_at) %> ago
  </dd>

  <dt>
    Updated at
  </dt>
  <dd>
    <%= time_ago_in_words(@shift.updated_at) %> ago
  </dd>
</dl>

<% if current_user.seniority == "Chief" %>
<a href="/delete_shift/<%= @shift.id %>">
  Delete shift
</a>
<% end %>

<hr>

<a href="/myshifts">
  Go back
</a>
